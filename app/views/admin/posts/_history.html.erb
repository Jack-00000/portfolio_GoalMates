<div class="row">
  <div class="d-flex flex-wrap justify-content-evenly col-12">
    <% posts.each do |post| %>
      <div class="card-title">
        　<%= link_to admin_post_path(post.id) do %>
          <%= link_to post.goal, admin_post_path(post.id), class: "text-dark" %>
        <% end %>
      </div>
    <% end %>
  </div>
</div>